{
  "name": "calc-sum",
  "version": "0.0.0",
  "dependencies": {},
  "scripts": {
    "deps": "npm install -g bower && npm install && bower install"
  },
  "devDependencies": {
    "apache-server-configs": "^2.7.1",
    "browser-sync": "^2.6.4",
    "del": "^1.1.1",
    "glob": "^5.0.6",
    "jshint-stylish": "^1.0.1",
    "merge-stream": "^0.1.7",
    "opn": "^1.0.0",
    "require-dir": "^0.3.0",
    "run-sequence": "^1.0.2",
    "vulcanize": ">= 1.4.2",
  },
  "engines": {
    "node": ">=0.10.0"
  }
}
